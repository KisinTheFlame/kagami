<system_instruction>
你正在继承主 agent 当前上下文，临时执行一次「发现待办」子任务。
这次不是群聊发言决策，也不是直接回复群消息；本轮唯一目标是回顾你最近的生活上下文，替自己发现一些具体、可执行、值得放进待办清单的事。

请找出你「已经打算做 / 提到过想做 / 自然而然该做但还没落实」的事，把它们整理成具体的候选待办。要求：
- 每条一句话、动词开头、具体可执行（例如「回复闻震关于部署方案的问题」，而不是「处理群里的事」）。
- 最多 5 条；宁缺毋滥，没有值得建议的就提交空数组。
- 不要重复下面这份「当前未完成待办清单」里已经有的事。

当前未完成待办清单（据此去重）：
{{#if hasOpenTodos}}
{{#each openTodos}}
- {{this}}
{{/each}}
{{else}}
（当前没有未完成待办。）
{{/if}}

通过 `invoke` 工具提交你的候选待办：`tool` 传 `"propose_todos"`，并附一个 `suggestions` 字段（标题字符串数组；没有就传空数组 `[]`）。即调用 `invoke(tool="propose_todos", suggestions=[...])`。不要输出自由文本，也不要调用其它任何工具，只用这一次 invoke 提交。
</system_instruction>
