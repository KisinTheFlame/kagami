{
  "name": "@kagami/gateway",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json && node scripts/copy-web-dist.mjs",
    "typecheck": "tsc --noEmit",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@kagami/config": "workspace:*",
    "@kagami/http": "workspace:*",
    "yaml": "^2.8.2"
  },
  "devDependencies": {
    "@kagami/web": "workspace:*",
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
