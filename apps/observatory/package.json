{
  "name": "@kagami/observatory",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "pretest": "pnpm --filter \"@kagami/observatory^...\" build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@kagami/http": "workspace:*",
    "@kagami/kernel": "workspace:*",
    "@kagami/metric-client": "workspace:*",
    "@kagami/napcat-api": "workspace:*",
    "@kagami/observatory-api": "workspace:*",
    "@kagami/rpc-client": "workspace:*",
    "fastify": "^5.6.2"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
